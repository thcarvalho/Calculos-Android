<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.calculos.terasgahd.calculos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImcActivity" />
        <activity android:name=".BhaskaraActivity" />
        <activity android:name=".MediaActivity" />
        <activity android:name=".PorcentagemActivity" />
        <activity android:name=".TabuadaActivity" />
        <activity android:name=".ResultadoTabuada"></activity>
    </application>

</manifest>